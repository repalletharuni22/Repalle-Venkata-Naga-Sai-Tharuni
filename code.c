#include <stdio.h>
int main(){
    int side,area;
    printf("Enter the side  : ");
    scanf("%d",&side);
    area=side*side;
    printf("The area of the square is : %d squarecm",area);
    return 0;
}